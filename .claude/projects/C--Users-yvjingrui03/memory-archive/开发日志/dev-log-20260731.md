---
name: dev-log-20260731
description: 2026-07-31 — StartMenu设置页搭建完成/卡包免费弃牌次数重新分配/底牌显示接入
metadata:
  node_type: memory
  type: project
  originSessionId: 87cd4b8b-a095-4e4f-a037-2ac9f126e98a
---

# 开发日志 2026-07-31

## Arithmetrick

- **StartMenu 设置页面搭建完成**：TabContainer 三栏（语言/操作/图像），返回按钮接 `MainMenu_Setting` 动画
- **卡包免费弃牌次数重分配**：红色 1 次，黄色 1 次，蓝色 2 次（原为蓝色 3/黄色 2/红色 1）
- **数据写入 DeckConfig.gd**：每套牌新增 `discard_free` 字段，`apply_deck_discard_free()` 改为从套牌配置读取，不再硬编码
- **底牌显示接入**：StartMenu 套牌选择界面新增「免费弃牌：N 次」显示
- **Bug 修复**：教程退出后再进入正常对局，牌库未正确重置——根因 `PlayField.is_playing` 在教程退出时未重置为 false，导致 `load_finish` 初始化逻辑被跳过。修复：`_cleanup_tutorial()` 中重置 `is_playing` + 清理残留 Card 节点
- **Bug 修复**：教程 `_play_anim()` 中 `animation_finished` 信号 lambda 未接收参数，Godot 4.x 严格检查报错，补 `func(_a)` 参数
- **三十三小队效果修正**：底数取值从静态 `base_number` 改为 `get_display_base_number()`，现在能吃到其他牌给本牌的底数加成

## 游戏

- 骨妹 A0 通关（STS2），继续体验解锁系统倒挂难度曲线
- 储君也通了一次，初始解锁牌组偏弱，但构筑成型后的满足感胜过竞技 FPS
- 正式版 STS2 已购入（Steam），作为设计参考书使用
