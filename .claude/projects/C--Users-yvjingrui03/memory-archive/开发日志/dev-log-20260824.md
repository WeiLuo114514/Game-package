---
name: dev-log-20260824
description: 2026-08-23/24 — 收藏页/InfoBoard修复/风险对冲/负片镀层/白牌5xxx重编号/SHOP_EXCLUDE/事件系统重构/竭力牌
metadata: 
  node_type: memory
  type: project
  originSessionId: 05545e16-273b-4793-a6a8-af996a5fc4d8
---

# 开发日志 2026-08-23/24

## StartMenu 收藏页（Collocation 我的收藏）
- 三 Tab 结构：历史记录/解锁内容/我的收藏，`collection_page.gd` 挂 `Collection` 根节点处理切换 + 刷新
- `card_container.gd` 加 `use_collected_deck` 开关（true 读 `GameData.collected_deck`，默认 draw_pile）
- 场景接线：History/Unlock/Collocation 按钮 → `_on_tab_pressed` bind(0/1/2)；`Collection/Button/return` → `_on_collection_return_pressed`；`_on_collection_pressed` 打开页面 + `on_page_opened()` 钩子
- `GameData.collected_deck` 目前无写入点，收藏系统数据链路已通待落地

## InfoBoard 初始底数/通关数不更新
- `info_board.gd update_info()` 早退 bug：货币未变时 `return` 跳过了静态文本（初始底数/已完成关卡/难度）
- 修复：静态文本移到货币早退之前
- `RoundMenu._on_info_board_mouse_entered` 调用了不存在的 `update_all_info()` → 改 `update_info()`

## 关卡王牌 风险对冲（L9012）
- 触发器 `ON_PRE_SETTLE` → `ON_SETTLE_STEP`（王牌结算阶段，走 `_execute_wild_card_phase` 的 tip/动画）
- 效果末尾加 `GameData.battle_info_changed.emit()` 立即刷新 InfoBoard 目标分/当前分

## 负片镀层系统（未来镀层复用基础）
- `WildConfig.Coating` 枚举（NONE/NEGATIVE）+ `WildData.coating`/`has_coating()`/`is_negative()`
- 23 张关卡王牌（LevelWildConfig 9xxx）全部标 `NEGATIVE`（config 第 7 元素），不占王牌计数
- `GameData.count_occupying_wilds()`（排除负片）/`can_add_wild()` 收敛所有容量判断（购买/特价/事件/龙的财宝/UI 计数）
- 未来镀层（Foil/Holo/Polychrome）在此扩展：加枚举值 + `has_coating()` 分支 + config 填元素

## 白色王牌结构重构（两次，最终态）
- **最终约定**：`0xxx=白（商店）`，`5xxx=事件/特殊产出`
- 白色段保留 **0001 回家 / 0004 激发潜能**（纯商店）；9 张非商店 → **5001-5009**（龙的财宝/预制魔法/平安符/未洛/巫女的吻/过期的烈酒/破损的藏宝图/垃圾·王/精灵秘酿）
- effect 脚本 `effect_0002-0011` → `effect_5001-5009`（含 .uid）；卡面图片 `000XB/T.png` → `500XB/T.png`（18 个 + .import）
- 引用更新：GameData（`cost_growth_locked` has_wild_card(5006)、`is_unrecyclable_wild` 5004/5005）、event_page（未洛 5004）、shop_page（特价区 has_wild_card(5007)）、EventConfig（9 处 wild_id）
- 白色段空位 0002/0003/0005-0011 留给未来填充的普通白色王牌（用户亲自设计）

## SHOP_EXCLUDE 数据驱动标记
- `WildConfig.Idx` 加 `SHOP_EXCLUDE`（=7，容错默认 false），9 张非商店牌 config 标 `true`
- `shop_page` 删除 `SHOP_WILD_BLACKLIST` 数组，`_refresh_wild_cards`/`_get_sale_wild_pool`（特价区）改读配置标记
- 龙的财宝（effect_5001）候选池也过滤 SHOP_EXCLUDE——「事件王牌只能事件获得」由配置统一约束

## 结算 WildSlot HBox + 弃牌统计
- `ending_page._populate_wild_cards` 移除 HBoxContainer（会拉伸卡尺寸），改复用 WildSlot 的固定间隔+局部坐标布局
- **超载 effect_1003 移除 `on_discard_occurred`**：「丢弃运算牌」只统计玩家主动弃置（ActiveCardContainer），结算自动弃置不计入；逐客令/吹又生（主动弃牌链补记）保留

## 事件系统重构（大）
- `EventConfig` 全量重写：17 事件按**关卡号段**编号（101-103 开局 / 301-303 Lv3 / 601-603 Lv6 / 901-903 Lv9 / 1201-1203 Lv12 / 1501 Lv15 / 1801 Lv18），旧配置（1-12、废弃 110-112）清理
- 新增 5 个 EventEffect：`GAIN_SPECIFIC_CARD`/`CLONE_CARD`/`SPEND_ALL_FILL_SLOTS`/`UPGRADE_CARD_WITH_SLOT`/`CLEAR_OWNED_MAGICS`
- **新运算牌「竭力」50003**（白 ×3，弃牌堆触发 -1 费+下回合-1）+ 新系统触发器 `ON_CARD_TO_DISCARD`（主动弃置 ActiveCardContainer + 结算飞出 PlayField 都触发）
- `select_card` 新增克隆模式 + 占印记升级模式（`add_magic(40001)` 占印记）；GameData 加 `pending_event_clone_count`/`pending_event_upgrade_with_slot`；RoundMenu 事件完成路由
- `GAIN_MAGIC` 补 `"high"` 池（价格 ≥12）；付费选项用 `[PAY_MONEY, 效果]` 组合（余额不足整选项失败）

## 待办/提醒
- 竭力牌（50003）与事件贴图（`资源/Picture/Event/{标题}.png`）多数缺失，需补图（16 事件插图 prompt 见 dev-log-20260816）
- 白色王牌填充：用户亲自设计，补进白色段空位（0002/0003/0005-0011），不标 SHOP_EXCLUDE 即自动进商店
- 竭力的弃牌触发已覆盖主动+自动，待实测
