---
name: dev-log-20260619
description: 2026-06-19/20 开发日志 — ID系统实现/品质铭牌/软上限/关卡王牌/粒子特效/Bug修复
metadata: 
  node_type: memory
  type: project
  originSessionId: 52d6c3eb-e346-415f-a02a-86fd8eca1665
---

2026-06-19~20 两天的开发工作记录。

## 1. 卡牌 ID 系统实现

格式：`品质-颜色+4位哈希`（例：`B-R7K3A`）

- `CardData.gd`：新增 `quality: int` 和 `card_uid: String`
- `GameData.gd`：`_deterministic_uid()` LCG 确定性哈希，同 card_id 永远同号；`_random_uid_suffix()` X 卡随机后缀；`mark_card_as_x()`；10 个丑陋字母→数字映射
- `CardUI.gd`：`QualityLabel.text = card_data.card_uid`
- 量产卡同型同号，X 化后唯一随机号

## 2. 品质铭牌

- 4 套 StyleBoxTexture：B/A/S/X_Panel.tres（`资源/texture/panel/`）
- `CardUI.gd` 按品质切换铭牌样式
- QualityIcon 已删除

## 3. 手牌软上限

- `draw_cards` 不再硬截断
- 结算结束从右侧弃掉超出 7 张的牌

## 4. 关卡王牌系统（UI 已接入）

- `level_wild_cards` 数组 + `generate_level_wild_cards()` + `refresh_level_wild_card()`
- InfoPage 三槽位 + 刷新按钮（费用递增）

## 5. 信号驱动的粒子特效

- `card_effect_triggered(color)` 信号
- BackgroundManager 连接并按颜色播放粒子

## 6. 王牌名称弹出标签

- 结算时王牌名称金色描边标签上浮+淡出

## 7. Bug 修复（7 个）

商店刷新/仓库报错/命中怀表/测不准/八方支援/春风/givecard

## 8. 设计讨论

- 关卡王牌 16 张草稿（正面/负面补偿/纯负面/Boss 专属）
- 超载→后续扩展、错误系统→王牌试水、数字性格→否决
