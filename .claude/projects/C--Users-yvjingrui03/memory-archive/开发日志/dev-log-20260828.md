---
name: dev-log-20260828
description: 2026-08-28 — 事件王牌ID修复/财富标签修复/图鉴页完成/WildCard静态模式/历史记录系统/王牌布局步长97
metadata: 
  node_type: memory
  type: project
  originSessionId: fb1cacab-b65c-483a-9efd-e0376d2efdc2
---

# 开发日志 2026-08-28

## 事件王牌重编号修复
- 白牌 5xxx 重编号后事件配置残留旧 ID：事件 102 预制的魔法 `5003002`→`5002`、601 龙的财宝 `5003001`→`5001`（`EventConfig.gd`）
- 防御：`event_page.gd:_exec_gain_specific_wild` 加校验，ID 不在 `wild_id_mapping` 则 `push_warning` 跳过

## WEALTH 财富标签修复
- 根因：`on_card_resolved` 只注册从未派发，财富处理器从未被调用
- 修复：`PlayField._execute_card_effect_phase` 在王牌 `ON_CARD_RESOLVED` 同一收集窗口补 `TagSystem.dispatch_one("on_card_resolved", cd, {...})`
- `TagSystem.gd` 财富 handler 重构为独立效果：`SettlementSequencer.submit_request("wealth_tag")`，execute 改钱、animate 飘金「财富 +1$」

## 图鉴页（StartMenu Collection → Illustrated Tab）
- `collection_page.gd` 加 `ILLUSTRATED` Tab、`IllMode {CARD, WILD, LEVEL_WILD}` 模式切换
- 运算牌用 DeckContainer（`card_container.gd` 加 `show_all_cards` 全卡模式 + `filter_color`/`search_text`）；王牌/关卡王牌复用场景预置 120 个 WildCard 对象池
- 信号入口：`_on_illustrated_card_filter_pressed(color)` / `_on_illustrated_wild_filter_pressed(color)` / `_on_illustrated_level_filter_pressed()` / `_on_illustrated_search_changed(text)`（用户手动接线）
- 效果展示区块用户暂缓（打算做异彩/镀层/标签说明）

## WildCard 静态展示实例差分
- `WildCardUI.init_wild_ui(data_id, load_effect=false)`：图鉴/历史用静态模式，跳过 `_try_load_effect`，`_reset_effect_for_display()` 重置 EffectNode 回基类脚本+清空数据引用（不显示统计数据）
- hover 提层：进入 `z_index=home_z_index+1`，退出归位（幂等不漂移）

## 错峰加载
- `card_container.gd:refresh_grid` 加 `_refresh_token` 令牌 + 每卡 `await process_frame`，旧构建作废
- 用户对 collection_page 的 wild grid 去掉了我的 token，保留末尾一个 await（尊重其改动）

## ShowCard 翻面改松开触发
- `ShowCard._gui_input` 把 `card_clicked.emit` 从按下移到松开；选中双击切换逻辑不变；card_desk_page/card_container 两处翻面统一生效

## 历史记录系统（完整+牌组+王牌，上限50）
- `GameData`：`history_records` + `load_history/save_history`（`user://history.json` JSON，项目首个 JSON 序列化）+ `finalize_history_record()`（ending_snapshot+ending_type+终局牌组/王牌，push_front+cap50）+ `_serialize_deck/_serialize_wild_cards`
- 漏斗选点：`ending_page.open()` 顶部调 `finalize_history_record()`（两条结局路径唯一汇合点）
- `collection_page.gd` History Tab：左列表逐条 Button（时间+模式+结果）、右详情标题/关卡/六统计/Seed/王牌/牌组；`_reconstruct_deck` 用 `GameData.new_OPcard` 重建
- 历史牌组卡片点击 → 共享 Container `DescriptionPage`（代码已接，场景节点待用户保存 + 手动连 gui_input 到 `_on_history_description_page_gui_input`）

## 王牌布局步长 97（三处统一）
- 根因：曾用卡宽 72 当步长，卡片贴边、溢出压缩到 30 导致重叠
- 修复：步长 = 卡宽 72 + 间距 25 = 97，放不下均匀压缩（`usable/count`），复刻 WildSlot 规则
- 结束页中心 (442.5,9)，历史页中心 (678.5,9)
