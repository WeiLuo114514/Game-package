---
name: dev-log-20260707
description: 2026-07-07 事件牌系统：商店事件牌商品/通用概率门/3个T1事件/BaseCardData底层重构/结算动画设计讨论
metadata: 
  node_type: memory
  type: project
  originSessionId: 4db33809-5ae1-4046-94b6-9a9b117a39bc
---

## 2026-07-07 全天

### 底层数据重构：stat.base → BaseCardData
- 新建 `BaseCardData.gd`（Resource）：init/bonus/next/val + recompute_val/reset_next
- 新建 `BaseCardConfig.gd`：默认底数牌 id=0, start_val=30
- GameData.stat.base 字典 → GameData.base_card: BaseCardData
- 全局 25 处 stat.base.xxx → base_card.xxx（7 文件，0 残留）

### 商店事件牌系统
- 新文件 `goods_event.gd`：完整 goods 脚本（双击购买/TIER_NAMES/品质标签/hover/init_data/fade_out）
- 场景 `goods_event.tscn`：用户制作，GoodsEvent 节点位于 Panel1/CardPage
- shop_page 集成：event_slot 引用 + _refresh_event_card（随机售价+品质分布）+ _on_event_purchased（扣钱→抽事件→打开EventPage）
- 事件牌不随商店刷新更新

### 事件牌独立池
- EventConfig.event_card_pool：3 级品质池，独立于强制事件 (ID 1-7)
- 通用概率门：`_execute_effect` 入口处检测 params.chance，任意效果零成本加概率
- GAIN_MONEY 支持 min/max 随机范围
- 新增 OPEN_RANDOM_BASE_PACK 效果（随机开 R/Y/B/G 基础包）
- 新增 APPLY_VARIANT 支持指定 variant_type
- 3 个 T1 事件：商队残骸(110) / 迷路的信使(111) / 垃圾桶淘金(112)

### 事件牌退场链路
- GameData.pending_event_is_card flag
- RoundMenu._event_back() 辅助函数：统一处理事件牌退场回商店
- 事件牌不记入 opened_event_ids（允许复抽）
- 延迟效果（删牌/打标签/清印记/开包）完成后也回商店
- pending_event_pack_type 支持指定开包类型

### 结算动画设计讨论（规划阶段）
- 四阶段：效果结算→王牌结算→运算结算→归位
- select_targets 与 apply 两段式分离：预期→确认认知节奏
- 动画逻辑放效果脚本内，PlayField 提供通用视觉工具
- 卡牌浮起/魔法闪光/焦点高亮/卡牌砸向底数

### 底数牌设计讨论（规划阶段）
- 初始底数做成一张牌：有牌面有效果，每套牌有自己的底数牌
- 四色特化：红=额外得分/黄=概率提高/蓝=免费弃牌/绿=更高初始费用
- 分期：一期数据层（已完成）→二期UI→三期效果→四期事件替换

### 魔法能量流 Shader
- `资源/Shader/mana_flow.gdshader`：右侧辉光+水平光带+浮动粒子
- `资源/Shader/flame_aura.gdshader`：火焰 shader（备选）
- PlayInfo.FireShader 驱动 intensity（0~1.0，3$ QUAD tween）+ glow_color（0.5s SINE tween）
- 动态颜色：场上卡牌颜色计数→dominant 自动切换辉光颜色
- 无牌默认套牌颜色，平局保留上一次颜色
- 左侧边缘始终平滑淡出（smoothstep），避免硬截断
- 旧 shader 从 代码/shader/ → 资源/Shader/ 目录迁移
